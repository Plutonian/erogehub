package com.goexp.galgame.data.task.mapreduce;

public class CleanGameTask {

    public static void main(String[] args) {

//        var tokenizer = Tokenizer.builder()
//                .mode(Tokenizer.Mode.SEARCH)
//                .build();

//        tokenizer.tokenize("白雪碧")
//                .stream()
//                .filter(token -> token.getBaseForm() != null)
//                .forEach(token -> {
//
//                    System.out.println(token.getSurfaceForm() + " {} " + token.getPartOfSpeech() + " {} " + token.isKnown());
//                });

//        var reg = Pattern.compile("[\\s〜]+[^\\s]+版");
//
//        var gameQuery = new GameService();
//        gameQuery.idsByBrand(21).stream().map(gameQuery::get).forEach(game -> {
//            var trueName = game.name.split("[〜\\s]")[0];
//
//            var words = tokenizer.tokenize(trueName).stream().map(Token::getSurfaceForm).collect(Collectors.joining(","));
//
//            System.out.printf("%-8d| %-60s | %-20s\n", game.id, game.name, words);
//        });
    }
}
